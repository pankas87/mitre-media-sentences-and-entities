require 'rspec'

# Look at the other request specs, the ones in the ETH NFT Ethereum
describe 'EntitiesController', type: :request do
  describe "#create_entity" do
    pending "creates a new entity with valid parameters and redirects back to the sentence page"
    pending "redirects back to the sentence page, setting some error messages"
  end
end